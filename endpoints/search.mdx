---
title: 'Search Submissions'
icon: 'code'
---

### URL
`https://phish.observer/api/search?query=<string>&skip=<number>`

```json Response
[
    {
        "id": "string",
        "url": "string",
        "favicon": "string",
        "effectiveUrl": "string",
        "realDomain": "string",
        "parsedDomain": "string",
        "status": "string",
        "screenshot": "string",
        "mobileScreenshot": "string",
        "api": "boolean",
        "node": "string",
        "timeline": [
            {
                "event": "string",
                "timestamp": "string"
            }
        ],
        "createdAt": "string",
        "assets": "number",
        "dns": "number",
        "abuseContacts": [
            "string"
        ],
        "tags": [
            "string"
        ],
        "scan": {
            "status": "string",
            "iok": [
                {
                    "kit": {
                        "tags": [
                            "string"
                        ],
                        "sortedTags": [
                            "string"
                        ],
                        "name": "string"
                    }
                }
            ]
        }
    }
]
```

The submission object for a search query is slightly truncated as a full object should be requested via the "Fetch submission" object. Most result keys are not used, so will not be returned.

